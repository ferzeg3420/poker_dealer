Make a diagram for the states and find good names for each.

Add an animation to transition between guesses.

Add an extra step so that the user gets to see the correct guess.
Something like make a guess, get results, and then click on continue.

Put a button to make guesses.

Make a functionality to select multiple players (A must)

Double check that straights are working.

Make unit tests just for discovery of how to do that in Python

Make an intro animation. 

Make an outro animation

Make a reset functionality

Make a cheasheet modal or sth

Make a leaderboard TOP 10 would be alright.
